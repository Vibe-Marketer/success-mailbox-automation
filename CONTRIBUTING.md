# Contributing

Keep changes provider-specific, credential-free, and covered by tests. Run `bun run typecheck`, `bun test`, and `bun run audit:contracts` before opening a pull request.

Do not add customer names, live workspace data, private Slack material, captured browser bundles, or real credentials.
